import { jest } from '@jest/globals';

export const trackLogin = jest.fn();
export const trackError = jest.fn();
export const trackUserSession = jest.fn();
export const setUserProperties = jest.fn(); 